# Downmix every playback stream to one channel while leaving capture untouched.
stream.rules = [
  {
    matches = [
      { media.class = "Stream/Output/Audio" }
    ]
    actions = {
      update-props = {
        audio.channels = 1
        audio.position = [ MONO ]
      }
    }
  }
]
