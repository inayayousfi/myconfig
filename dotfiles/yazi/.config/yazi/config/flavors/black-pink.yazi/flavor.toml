#----------------------------------------------------------
#--  🖤🌸 Black & Pink Yazi Flavor
#----------------------------------------------------------

[mgr]
cwd = { fg = "#ff4ead" }

# Hovered
hovered = { bg = "#ff4ead", fg = "#f0f2f7", bold = true }
preview_hovered = { bg = "#ff4ead", underline = true }

# Find
find_symbol = " Finding: "
find_keyword = { fg = "#ff4ead", bold = true, italic = true, underline = true }
find_position = { fg = "#a78bfa", bg = "reset", bold = true, italic = true }

# Symlink
symlink_target = { fg = "#60a5fa", italic = true }

# Marker
marker_copied = { fg = "#4ade80", bg = "#4ade80" }
marker_cut = { fg = "#f87171", bg = "#f87171" }
marker_marked = { fg = "#a78bfa", bg = "#a78bfa" }
marker_selected = { fg = "#ff4ead", bg = "#ff4ead" }

# Count
count_copied = { fg = "#000000", bg = "#4ade80" }
count_cut = { fg = "#000000", bg = "#f87171" }
count_selected = { fg = "#000000", bg = "#ff4ead" }

# Border
border_symbol = "│"
border_style = { fg = "#1a1a1a" }

# Tabs
[tabs]
active = { fg = "#000000", bg = "#ff4ead", bold = true }
inactive = { fg = "#a0aabe", bg = "#0a0a0a" }

# Separator
sep_inner = { open = "", close = "" }
sep_outer = { open = "", close = "" }

# Modes
[mode]
# Normal mode
normal_main = { fg = "#000000", bg = "#ff4ead", bold = true }
normal_alt = { fg = "#ff4ead", bg = "#111111" }

# Select mode
select_main = { fg = "#000000", bg = "#a78bfa", bold = true }
select_alt = { fg = "#a78bfa", bg = "#111111" }

# Unset mode
unset_main = { fg = "#000000", bg = "#f87171", bold = true }
unset_alt = { fg = "#f87171", bg = "#111111" }

# Status bar
[status]
overall = {}
sep_left = { open = "", close = "" }
sep_right = { open = "", close = "" }

# Permissions
perm_sep = { fg = "#4a4f5e" }
perm_type = { fg = "#ff4ead" }
perm_read = { fg = "#facc15" }
perm_write = { fg = "#f87171" }
perm_exec = { fg = "#4ade80" }

# Progress
progress_label = { fg = "#f0f2f7", bold = true }
progress_normal = { fg = "#60a5fa", bg = "#111111" }
progress_error = { fg = "#f87171", bg = "#111111" }

# Which
[which]
cols = 3
mask = { bg = "#000000" }
cand = { fg = "#60a5fa" }
rest = { fg = "#4a4f5e" }
desc = { fg = "#ff4ead" }
separator = "  "
separator_style = { fg = "#2a2d36" }

# Confirmation
[confirm]
border = { fg = "#ff4ead" }
title = { fg = "#ff4ead" }
content = { fg = "#d0d6e0" }
list = { fg = "#d0d6e0" }
btn_yes = { bg = "#ff4ead", fg = "#ff4ead", bold = true, reversed = true }
btn_no = { fg = "#a0aabe" }
btn_labels = ["  [Y]es  ", "  (N)o  "]

# Spotter
[spot]
border = { fg = "#1a1a1a" }
title = { fg = "#f0f2f7" }

# Table
tbl_col = { fg = "#a0aabe" }
tbl_cell = { fg = "#f0f2f7", bg = "#111111", reversed = true }

# Notifications
[notify]
title_info = { fg = "#60a5fa" }
title_warn = { fg = "#facc15" }
title_error = { fg = "#f87171" }

# Icons
icon_info = ""
icon_warn = ""
icon_error = ""

# Picker
[pick]
border = { fg = "#ff4ead" }
active = { fg = "#ff4ead", bold = true }
inactive = { fg = "#a0aabe" }

# Input
[input]
border = { fg = "#ff4ead" }
title = { fg = "#d0d6e0" }
value = { fg = "#f0f2f7" }
selected = { bg = "#ff4ead", fg = "#ff4ead" }

# Completion
[cmp]
border = { fg = "#1a1a1a" }
active = { bg = "#ff4ead", fg = "#f0f2f7" }
inactive = { fg = "#a0aabe" }

# Icons
icon_file = ""
icon_folder = ""
icon_command = ""

# Tasks
[tasks]
border = { fg = "#1a1a1a" }
title = { fg = "#f0f2f7" }
hovered = { fg = "#ff4ead", underline = true }

# Help menu
[help]
on = { fg = "#ff4ead" }
run = { fg = "#a78bfa" }
desc = { fg = "#d0d6e0" }
hovered = { bg = "#ff4ead", bold = true }
footer = { fg = "#000000", bg = "#f0f2f7" }

# File-specific styles 
[filetype]
rules = [
  # Directories
  { name = "*/", fg = "#ff4ead", bold = true },
  { mime = "inode/directory", fg = "#ff4ead", bold = true },

  # Images
  { mime = "image/*", fg = "#facc15" },

  # Media
  { mime = "{audio,video}/*", fg = "#4ade80" },

  # Archives
  { mime = "application/{zip,rar,7z*,tar,gzip,xz,zstd,bzip*,lzma,compress,archive,cpio,arj,xar,ms-cab*}", fg = "#ff4ead" },

  # Documents
  { mime = "application/{pdf,doc,rtf}", fg = "#60a5fa" },

  # Empty files
  { mime = "inode/empty", fg = "#4a4f5e" },

  # Special files
  { url = "*", is = "orphan", bg = "#f87171" },
  { url = "*", is = "exec", fg = "#4ade80" },

  # Dummy files
  { url = "*", is = "dummy", bg = "#f87171" },
  { url = "*/", is = "dummy", bg = "#f87171" },

  # Fallback
  { url = "*/", fg = "#d0d6e0", bold = true },
]
