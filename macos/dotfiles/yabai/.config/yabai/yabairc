# Layout
yabai -m config layout bsp

# Window padding
yabai -m config window_gap 4

# External bar (sketchybar)
yabai -m config external_bar all:0:32

# Placement
yabai -m config window_placement second_child

# Mouse
yabai -m config mouse_modifier alt

# modify window shadows (default: on, options: on, off, float)
# example: show shadows only for floating windows
yabai -m config window_shadow float

# window opacity (default: off)
# example: render all unfocused windows with 90% opacity
yabai -m config window_opacity on
yabai -m config active_window_opacity 1.0
yabai -m config normal_window_opacity 0.5

# Ignore certain applications
yabai -m rule --add app='^MuMu Android Device$' manage=off

# Borders
options=(
 style=round
 width=4.0
 hidpi=on
 active_color=0xffa6e22e
 inactive_color=0xff414550
)

borders "${options[@]}" &
